import 'package:flutter/material.dart';

class AppColors {
  static const neon = Color(0xFF39FF14);
  static const bg = Color(0xFF0D0F14);
  static const surface = Color(0xFF1A1D24);
  static const surfaceLight = Color(0xFF252830);
  static const textMuted = Color(0x66FFFFFF);
  static const textHint = Color(0x3DFFFFFF);
}

class AppText {
  static const appName = "REFACCIONARIA";
  static const appTitle = "LOS AMIGOS CORE";
}