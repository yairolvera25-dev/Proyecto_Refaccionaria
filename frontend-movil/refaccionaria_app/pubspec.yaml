name: refaccionaria_app
description: "Sistema móvil para refaccionaria"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.2
  flutter_dotenv: ^5.1.0
  dio: ^5.4.3+1
  provider: ^6.1.2
  fl_chart: ^0.68.0
  shared_preferences: ^2.3.2
  pdf: ^3.10.8
  printing: ^5.12.0
  excel: ^4.0.3
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env